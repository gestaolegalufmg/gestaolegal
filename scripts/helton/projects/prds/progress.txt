# Helton Progress Log
Started: sáb. 05 set 2026 08:33:32 -03
---
## Codebase Patterns
- Guarda de unidade mora no **service**, não no controller nem no repositório de
  domínio: método privado `_caso_da_unidade_ativa(caso_id)` que chama
  `CasoRepository.find_by_id(caso_id, unidade_id=RequestContext.get_unidade_ativa())`.
  Já existe em `caso_service.py`, `evento_service.py` e agora em `processo_service.py`.
- Caso de outra unidade responde **404 (NotFoundException), nunca 403** — 403 vazaria
  a existência do caso. Nas leituras que devolvem `| None` o service devolve `None` e
  quem levanta o 404 é o controller.
- `make test` falha com `ModuleNotFoundError: flask` até rodar `uv sync --extra extra`
  (pytest vive no grupo/extra `extra`, o grupo `dev` está vazio). É o que o CI faz.
- Fixtures de unidade em `tests/api/conftest.py`: `auth_headers` (admin, X-Unidade-Id=1/BH)
  e `auth_headers_nl` (mesmo admin, X-Unidade-Id=2/Nova Lima). O admin pertence às duas
  unidades — é com esse par que se prova isolamento sem confundir com falta de permissão.

---

## 2026-09-05 - f1-processo-guarda-unidade
- `ProcessoService` passou a recusar caso que não é da unidade ativa. Guarda aplicada em
  `search_by_caso` (raise NotFoundException), `validate_processo_for_caso` (return None,
  o controller converte em 404) e `create` (raise NotFoundException). Isso cobre as seis
  rotas de processo do `caso_controller`, inclusive o DELETE `@authorized("admin")`.
- Arquivos: `gestaolegal/services/processo_service.py`, `tests/api/test_processo_api.py`.
- Dois testes novos: um percorre as cinco chamadas HTTP com o header da unidade errada e
  exige 404; outro confirma que o mesmo caso, pela unidade em que foi aberto, segue
  listando, criando, lendo, editando e inativando.
- Suíte: 345 passed.
- **Learnings for future iterations:**
  - O controller de processo não mudou de assinatura: quem recusa é o service. Adicionar
    guarda no controller duplicaria a regra e sairia do padrão da casa.
  - `search_by_caso` levanta a exceção em vez de devolver página vazia porque página vazia
    é indistinguível de "caso sem processos" — o 404 é o que fecha o vazamento.
---
